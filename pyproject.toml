[project]
name = "pr-visuals-reference-implementation"
version = "0.1.0"
description = "Reference implementation: visual PR descriptions for a fresh Django project, one theme per branch"
requires-python = ">=3.12"
dependencies = [
    "django==6.0.7",
]
